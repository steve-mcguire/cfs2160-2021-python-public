# simple addition, ads two args (x and y), performs the addition and returns the result.
def addition(x, y):
    result = x + y
    return result


# simple multiplication, ads two args (x and y), performs the multiplication and returns the result.
def multiplication(x, y):
    r = x * y
    return r


# simple division, ads two args (x and y), performs the division and returns the result.
def division(x, y):
    return x / y


# testing the functions, call the function with values for x and y and print the results.
print(addition(10, 6))
print(multiplication(10, 5))
print(division(10,5 ))